services:
  video_labeler:
    build: .
    container_name: video_labeler
    ports:
      - "${VIDEO_LABELER_PORT:-8000}:8000"
    volumes:
      # Bind mount data directory to persist studies and uploads
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
